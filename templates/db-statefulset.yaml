apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: db
spec:
  serviceName: "db"
  replicas: 1
  selector:
    matchLabels:
      app: db  # <--- THIS MUST MATCH THE TEMPLATE BELOW
  template:
    metadata:
      labels:
        app: db  # <--- THIS MUST MATCH THE SELECTOR ABOVE
    spec:
      containers:
        - name: postgres
          # ... (rest of config)
