apiVersion: apps/v1
kind: StatefulSet # or Deployment
metadata:
  name: n8n
spec:
  serviceName: "n8n"
  replicas: 1
  selector:
    matchLabels:
      app: n8n  # <--- THIS MUST MATCH THE TEMPLATE BELOW
  template:
    metadata:
      labels:
        app: n8n  # <--- THIS MUST MATCH THE SELECTOR ABOVE
    spec:
      containers:
        - name: n8n
          # ... (rest of config)
